# docker/DockerfileBackend
FROM node:22

WORKDIR /app

COPY package.json package-lock.json ./
RUN ls -la /app && test -f /app/package-lock.json && npm ci

COPY . .
RUN rm -rf dist
RUN npm run build

CMD ["node", "dist/main.js"]